- job_name: appointment-api
    scrape_interval: 10s
    metrics_path: /metrics
    static_configs:
      - targets:
          - medicore-appointment:8080       # Docker container (Compose)
          - host.docker.internal:5003       # host machine (dotnet run)
        labels:
          service: appointment
          env: local